# Copy to .env and fill in. NEVER commit the real .env.
OPENAI_API_KEY=your-key-here
# Optional overrides:
OPENAI_MODEL=gpt-4o
# Force deterministic offline mode even if a key is present:
DEAL_DNA_OFFLINE=0
